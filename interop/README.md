# interop
Phase 1 placeholder for py-ipfs-lite <-> go-ipfs-lite interoperability assets.

Planned in Phase 6:
- smoke tests for `py -> go` and `go -> py` file transfer
- reproducible scripts/container workflow for interop runs
