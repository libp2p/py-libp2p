Fixed multiple bugs in bootstrap discovery: DNS-resolved addresses now properly have /p2p component stripped (with ValueError handling for addresses without /p2p), connection timeout handler is now functional (replaced trio.move_on_after with trio.fail_after), allow_ipv6 parameter is now enforced in address filtering, ID.from_string is used instead of ID.from_base58 for peer ID parsing, Exception is replaced with TypeError in except clause, connection_timeout is now float type, and missing type annotations on dns instance variables are added.
