Add a per-query timeout to Kademlia DHT network lookups. A peer that opened the query stream but never responded could previously block ``find_peer``, ``provide``, ``find_providers`` and routing-table refresh indefinitely; each per-peer query is now bounded by ``QUERY_TIMEOUT``.
