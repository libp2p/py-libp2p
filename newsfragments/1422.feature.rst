Made the per-bucket subnet-diversity limit runtime-configurable via a ``max_peers_per_subnet`` argument on ``RoutingTable``, ``KBucket`` and ``KadDHT`` (issue #1422), and added an opt-in table-wide IP-group cap (``max_peers_per_subnet_table``) limiting peers sharing one subnet across all k-buckets (issue #1421).
