De-flake the ``dht_pair`` fixture in ``tests/core/kad_dht/test_kad_dht.py``: replace the fixed ``trio.sleep(0.1)`` plus one-shot ``peer_in_table`` assertions with a bounded poll (``trio.fail_after(10)``) that waits until both nodes appear in each other's routing table. The entries are populated by the background refresh manager (time-driven), so under CI load the fixed window raced and setup failed with a spurious ``AssertionError``.
