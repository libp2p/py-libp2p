Close leaked sockets in test teardowns across the ``io``, ``transport`` (tcp/websocket), ``bitswap``, ``identity``, ``discovery``, and ``network`` core suites — tests that dialed connections, started listeners, or created hosts without closing them on teardown. This clears the bulk of the residual ``ResourceWarning: unclosed <socket.socket>`` warnings tracked in #1498 (io/bitswap/identity/discovery to zero). The webrtc/quic teardown residual, one pubsub case, and one network case remain before the suite-wide ``filterwarnings = ["error::ResourceWarning"]`` guard can be enabled.
