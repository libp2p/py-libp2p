Multicodec integration for Bitswap CIDs: CIDv1 now uses proper varint-encoded codec prefixes (via ``add_prefix()``). Codecs with values < 128 (e.g. ``raw``, ``dag-pb``) remain backward compatible; codecs ≥ 128 (e.g. ``dag-jose``, ``dag-json``) use multi-byte varint encoding and represent a breaking change for existing CIDv1 bytes. New helpers: ``detect_cid_encoding_format()``, ``recompute_cid_from_data()``, and ``analyze_cid_collection()``. See the codec documentation in the docs for migration guidance.
