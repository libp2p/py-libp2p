"""
Entry point for the path handling demo.

This allows running the example as a CLI tool via `python -m examples.path_handling_demo`
or as a console script (path-handling-demo) if registered in pyproject.toml.
"""
from examples.path_handling import main

if __name__ == "__main__":
    main()
