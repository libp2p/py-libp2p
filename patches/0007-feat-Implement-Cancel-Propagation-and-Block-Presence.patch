From 000368c7d49a5f7bdec66019f623d06b2c64fa4a Mon Sep 17 00:00:00 2001
From: sumanjeet0012 <sumanjeet0012@gmail.com>
Date: Sat, 25 Jul 2026 11:06:03 +0530
Subject: [PATCH 07/13] feat: Implement Cancel Propagation and Block Presence
 Manager

---
 libp2p/bitswap/client.py            | 100 ++++++++++++++--------------
 libp2p/bitswap/presence.py          |  78 ++++++++++++++++++++++
 tests/core/bitswap/test_client.py   |   9 +--
 tests/core/bitswap/test_presence.py |  53 +++++++++++++++
 4 files changed, 186 insertions(+), 54 deletions(-)
 create mode 100644 libp2p/bitswap/presence.py
 create mode 100644 tests/core/bitswap/test_presence.py

diff --git a/libp2p/bitswap/client.py b/libp2p/bitswap/client.py
index bab38c65..1505e0e5 100644
--- a/libp2p/bitswap/client.py
+++ b/libp2p/bitswap/client.py
@@ -46,6 +46,7 @@ from .errors import (
 )
 from .messages import create_message, create_wantlist_entry
 from .pb.bitswap_pb2 import Message
+from .presence import BlockPresenceManager
 from .provider_query import ProviderQueryManager
 
 logger = logging.getLogger(__name__)
@@ -67,18 +68,17 @@ class BitswapClient:
         block_store: BlockStore | None = None,
         protocol_version: str = BITSWAP_PROTOCOL_V120,
         provider_query_manager: ProviderQueryManager | None = None,
-    ):
+        presence_ttl: float = 60.0,
+    ) -> None:
         """
-        Initialize Bitswap client.
+        Initialize a Bitswap client.
 
         Args:
             host: The libp2p host
-            block_store: Block storage backend (defaults to in-memory)
-            protocol_version: Preferred protocol version (defaults to v1.2.0)
-            provider_query_manager: Optional ProviderQueryManager for automatic
-                DHT-based provider discovery.  When supplied,
-                ``get_block()`` will query the DHT for providers before
-                broadcasting to all connected peers.
+            block_store: The block store to use (defaults to MemoryBlockStore)
+            protocol_version: The Bitswap protocol version string to prefer
+            provider_query_manager: Optional manager to handle DHT provider queries
+            presence_ttl: Time-to-live for block presence tracking (default 60s)
 
         """
         self.host = host
@@ -98,12 +98,10 @@ class BitswapClient:
             PeerID, dict[CIDObject, dict[str, Any]]
         ] = {}  # peer -> wantlist
         self._pending_requests: dict[CIDObject, set[trio.Event]] = {}  # CID -> event
-        # CID -> peers who sent DontHave
-        self._dont_have_responses: dict[CIDObject, set[PeerID]] = {}
         self._peer_protocols: dict[PeerID, str] = {}  # peer -> negotiated protocol
-        self._expected_blocks: dict[
-            PeerID, set[CIDObject]
-        ] = {}  # peer -> expected CIDs
+
+        self.presence_manager = BlockPresenceManager(ttl_seconds=presence_ttl)
+
         self._nursery: trio.Nursery | None = None
         self._started = False
 
@@ -127,8 +125,16 @@ class BitswapClient:
             )
 
         self._started = True
+        if self._nursery is not None:
+            self._nursery.start_soon(self._presence_cleanup_loop)
         logger.info(f"Bitswap client started (protocol: {self.protocol_version})")
 
+    async def _presence_cleanup_loop(self) -> None:
+        """Periodic background task to clean up expired presence state."""
+        while self._started:
+            await trio.sleep(10)
+            self.presence_manager.cleanup_expired()
+
     async def stop(self) -> None:
         """Stop the Bitswap client."""
         if not self._started:
@@ -142,9 +148,10 @@ class BitswapClient:
         self._wantlist.clear()
         self._peer_wantlists.clear()
         self._pending_requests.clear()
-        self._dont_have_responses.clear()
         self._peer_protocols.clear()
-        self._expected_blocks.clear()
+        self.presence_manager = BlockPresenceManager(
+            ttl_seconds=self.presence_manager.ttl
+        )
         logger.info("Bitswap client stopped")
 
     def set_nursery(self, nursery: trio.Nursery) -> None:
@@ -291,11 +298,10 @@ class BitswapClient:
                             del self._pending_requests[cid_obj]
                     if cid_obj in self._wantlist:
                         del self._wantlist[cid_obj]
-                    if cid_obj in self._dont_have_responses:
-                        del self._dont_have_responses[cid_obj]
+                    self.presence_manager.remove_dont_have(cid_obj)
                     # Clean up expected_blocks for all peers
-                    for pid in list(self._expected_blocks.keys()):
-                        self._expected_blocks[pid].discard(cid_obj)
+                    for pid in self.presence_manager.get_expected_peers(cid_obj):
+                        self.presence_manager.remove_have(pid, cid_obj)
 
         return results
 
@@ -500,10 +506,10 @@ class BitswapClient:
                 self._pending_requests[cid].discard(event)
                 if not self._pending_requests[cid]:
                     del self._pending_requests[cid]
-            if cid in self._dont_have_responses:
-                del self._dont_have_responses[cid]
-            for pid in list(self._expected_blocks.keys()):
-                self._expected_blocks[pid].discard(cid)
+            self.presence_manager.remove_dont_have(cid)
+            # Clean up expected_blocks for all peers
+            for pid in self.presence_manager.get_expected_peers(cid):
+                self.presence_manager.remove_have(pid, cid)
 
         if error:
             raise error
@@ -1137,7 +1143,7 @@ class BitswapClient:
         logger.info(f"Processing {len(blocks)} blocks (v1.0.0) from peer {peer_id_str}")
 
         # Get the CIDs we're expecting from this peer
-        expected_cids = self._expected_blocks.get(peer_id, set()).copy()
+        expected_cids = self.presence_manager.get_expected_for_peer(peer_id)
         logger.info(f"Expected {len(expected_cids)} blocks from this peer")
         logger.info("Expected CIDs:")
         for i, cid in enumerate(expected_cids):
@@ -1174,15 +1180,13 @@ class BitswapClient:
                 logger.info("  ✓ Stored successfully")
 
                 # Remove from expected blocks for all peers
-                for pid in self._expected_blocks:
-                    if matched_cid in self._expected_blocks[pid]:
-                        self._expected_blocks[pid].discard(matched_cid)
-                        pid_str = (
-                            str(pid)[:16] if hasattr(pid, "__str__") else "unknown"
-                        )
-                        logger.info(
-                            f"  ✓ Removed from expected blocks for peer {pid_str}"
-                        )
+                self.presence_manager.remove_have_from_all(matched_cid)
+                pid_str = (
+                    str(peer_id)[:16] if hasattr(peer_id, "__str__") else "unknown"
+                )
+                logger.info(
+                    f"  ✓ Removed from expected blocks for peer {pid_str}"
+                )
 
                 # Notify pending requests
                 if matched_cid in self._pending_requests:
@@ -1190,6 +1194,9 @@ class BitswapClient:
                     logger.info(f"  ✓ Notifying {pending_count} pending requests")
                     for event in self._pending_requests[matched_cid]:
                         event.set()
+
+                    if self._started and self._nursery:
+                        self._nursery.start_soon(self.cancel_want, matched_cid)
             else:
                 logger.error("  ✗ NO MATCH FOUND!")
                 logger.error("  Block doesn't match any expected CID")
@@ -1202,7 +1209,7 @@ class BitswapClient:
         logger.info("")
         logger.info("=" * 70)
         logger.info("Block processing complete. Remaining expected blocks:")
-        remaining = self._expected_blocks.get(peer_id, set())
+        remaining = self.presence_manager.get_expected_for_peer(peer_id)
         if remaining:
             logger.warning(f"  Still waiting for {len(remaining)} blocks:")
             for i, cid in enumerate(remaining):
@@ -1230,8 +1237,7 @@ class BitswapClient:
             )
 
             # Remove from expected blocks for all peers
-            for peer_id in self._expected_blocks:
-                self._expected_blocks[peer_id].discard(cid)
+            self.presence_manager.remove_have_from_all(cid)
 
             # Notify pending requests
             if cid in self._pending_requests:
@@ -1242,6 +1248,9 @@ class BitswapClient:
                 )
                 for event in self._pending_requests[cid]:
                     event.set()
+
+                if self._started and self._nursery:
+                    self._nursery.start_soon(self.cancel_want, cid)
             else:
                 logger.debug(
                     f"No pending request for "
@@ -1270,17 +1279,12 @@ class BitswapClient:
             )
 
             if has_block:
-                # Peer has the block - we can expect it to arrive soon
-                # Track which peer has it
-                if peer_id not in self._expected_blocks:
-                    self._expected_blocks[peer_id] = set()
-                self._expected_blocks[peer_id].add(cid)
+                # We are sending directly to this peer, so we expect it from them
+                self.presence_manager.add_have(peer_id, cid)
             else:
                 # DontHave - peer confirms they don't have this block
                 # Track DontHave responses for metrics/optimization
-                if cid not in self._dont_have_responses:
-                    self._dont_have_responses[cid] = set()
-                self._dont_have_responses[cid].add(peer_id)
+                self.presence_manager.add_dont_have(cid, peer_id)
 
                 logger.info(
                     f"  ℹ️  Peer {peer_id} doesn't have block "
@@ -1393,17 +1397,13 @@ class BitswapClient:
             presence_type = presence.type
 
             if presence_type == 0:  # Have
-                if peer_id not in self._expected_blocks:
-                    self._expected_blocks[peer_id] = set()
-                self._expected_blocks[peer_id].add(cid)
+                self.presence_manager.add_have(peer_id, cid)
                 logger.debug(
                     f"[1.3.0] Peer {peer_id} has block "
                     f"{format_cid_for_display(cid, max_len=16)}"
                 )
             elif presence_type == 1:  # DontHave
-                if cid not in self._dont_have_responses:
-                    self._dont_have_responses[cid] = set()
-                self._dont_have_responses[cid].add(peer_id)
+                self.presence_manager.add_dont_have(peer_id, cid)
                 logger.info(
                     f"[1.3.0] Peer {peer_id} doesn't have block "
                     f"{format_cid_for_display(cid, max_len=16)}"
diff --git a/libp2p/bitswap/presence.py b/libp2p/bitswap/presence.py
new file mode 100644
index 00000000..ca8e0cf1
--- /dev/null
+++ b/libp2p/bitswap/presence.py
@@ -0,0 +1,78 @@
+import time
+from typing import Dict
+
+from libp2p.peer.id import ID as PeerID
+
+from .cid import CIDObject
+
+
+class BlockPresenceManager:
+    """Manages tracking of which peers have or don't have specific blocks with TTL."""
+    def __init__(self, ttl_seconds: float = 60.0):
+        self.ttl = ttl_seconds
+        # peer_id -> cid -> timestamp
+        self._have: Dict[PeerID, Dict[CIDObject, float]] = {}
+        # cid -> peer_id -> timestamp
+        self._dont_have: Dict[CIDObject, Dict[PeerID, float]] = {}
+
+    def add_have(self, peer_id: PeerID, cid: CIDObject) -> None:
+        if peer_id not in self._have:
+            self._have[peer_id] = {}
+        self._have[peer_id][cid] = time.time()
+
+    def add_dont_have(self, peer_id: PeerID, cid: CIDObject) -> None:
+        if cid not in self._dont_have:
+            self._dont_have[cid] = {}
+        self._dont_have[cid][peer_id] = time.time()
+
+    def remove_have(self, peer_id: PeerID, cid: CIDObject) -> None:
+        if peer_id in self._have and cid in self._have[peer_id]:
+            del self._have[peer_id][cid]
+            if not self._have[peer_id]:
+                del self._have[peer_id]
+
+    def remove_dont_have(self, cid: CIDObject) -> None:
+        if cid in self._dont_have:
+            del self._dont_have[cid]
+
+    def get_expected_peers(self, cid: CIDObject) -> set[PeerID]:
+        """Get peers that are expected to have the block."""
+        return {p for p in self._have if cid in self._have[p]}
+
+    def get_dont_have_peers(self, cid: CIDObject) -> set[PeerID]:
+        """Get peers that explicitly responded with DONT_HAVE."""
+        return set(self._dont_have.get(cid, {}).keys())
+
+    def get_expected_cids_for_peer(self, peer_id: PeerID) -> set[CIDObject]:
+        """Get all CIDs expected from a specific peer."""
+        return set(self._have.get(peer_id, {}).keys())
+
+    def get_expected_for_peer(self, peer_id: PeerID) -> set[CIDObject]:
+        """Alias for get_expected_cids_for_peer."""
+        return self.get_expected_cids_for_peer(peer_id)
+
+    def remove_have_from_all(self, cid: CIDObject) -> None:
+        """Remove a CID from all peers' expected blocks."""
+        for peer_id in list(self._have.keys()):
+            if cid in self._have[peer_id]:
+                del self._have[peer_id][cid]
+                if not self._have[peer_id]:
+                    del self._have[peer_id]
+
+    def cleanup_expired(self) -> None:
+        now = time.time()
+        # Clean up _have
+        for peer_id in list(self._have.keys()):
+            for cid in list(self._have[peer_id].keys()):
+                if now - self._have[peer_id][cid] > self.ttl:
+                    del self._have[peer_id][cid]
+            if not self._have[peer_id]:
+                del self._have[peer_id]
+
+        # Clean up _dont_have
+        for cid in list(self._dont_have.keys()):
+            for peer_id in list(self._dont_have[cid].keys()):
+                if now - self._dont_have[cid][peer_id] > self.ttl:
+                    del self._dont_have[cid][peer_id]
+            if not self._dont_have[cid]:
+                del self._dont_have[cid]
diff --git a/tests/core/bitswap/test_client.py b/tests/core/bitswap/test_client.py
index 5e7faf59..10d78fc0 100644
--- a/tests/core/bitswap/test_client.py
+++ b/tests/core/bitswap/test_client.py
@@ -409,16 +409,17 @@ class TestBitswapClientBugFixes:
 
         # Simulate expected block setup and dont_have
         peer_id = PeerID(b"peer1")
-        client._expected_blocks[peer_id] = {cid_obj}
-        client._dont_have_responses[cid_obj] = {peer_id}
+        client.presence_manager.add_have(peer_id, cid_obj)
+        client.presence_manager.add_dont_have(peer_id, cid_obj)
 
         with pytest.raises(BitswapTimeoutError):
             await client.get_block(cid, timeout=0.1)
 
         assert cid_obj not in client._pending_requests
         assert cid_obj not in client._wantlist
-        assert cid_obj not in client._dont_have_responses
-        assert cid_obj not in client._expected_blocks.get(peer_id, set())
+        assert peer_id not in client.presence_manager.get_dont_have_peers(cid_obj)
+        expected_cids = client.presence_manager.get_expected_cids_for_peer(peer_id)
+        assert cid_obj not in expected_cids
 
     @pytest.mark.trio
     async def test_broadcast_limit(self):
diff --git a/tests/core/bitswap/test_presence.py b/tests/core/bitswap/test_presence.py
new file mode 100644
index 00000000..b6bc1ed9
--- /dev/null
+++ b/tests/core/bitswap/test_presence.py
@@ -0,0 +1,53 @@
+import time
+
+from libp2p.bitswap.cid import compute_cid_v1
+from libp2p.bitswap.presence import BlockPresenceManager
+from libp2p.peer.id import ID as PeerID
+
+
+class TestBlockPresenceManager:
+    def test_add_and_remove(self):
+        manager = BlockPresenceManager(ttl_seconds=60.0)
+        peer1 = PeerID(b"peer1")
+        cid1 = compute_cid_v1(b"data1")
+
+        manager.add_have(peer1, cid1)
+        assert cid1 in manager.get_expected_cids_for_peer(peer1)
+        assert peer1 in manager.get_expected_peers(cid1)
+
+        manager.add_dont_have(peer1, cid1)
+        assert peer1 in manager.get_dont_have_peers(cid1)
+
+        manager.remove_have(peer1, cid1)
+        assert cid1 not in manager.get_expected_cids_for_peer(peer1)
+
+        manager.remove_dont_have(cid1)
+        assert peer1 not in manager.get_dont_have_peers(cid1)
+
+    def test_cleanup_expired(self, monkeypatch):
+        manager = BlockPresenceManager(ttl_seconds=1.0)
+        peer1 = PeerID(b"peer1")
+        cid1 = compute_cid_v1(b"data1")
+        cid2 = compute_cid_v1(b"data2")
+
+        # Mock time to T=0
+        monkeypatch.setattr(time, "time", lambda: 0.0)
+        manager.add_have(peer1, cid1)
+        manager.add_dont_have(peer1, cid1)
+
+        # Mock time to T=0.5 (Not expired)
+        monkeypatch.setattr(time, "time", lambda: 0.5)
+        manager.add_have(peer1, cid2)
+        manager.cleanup_expired()
+
+        assert cid1 in manager.get_expected_cids_for_peer(peer1)
+        assert cid2 in manager.get_expected_cids_for_peer(peer1)
+        assert peer1 in manager.get_dont_have_peers(cid1)
+
+        # Mock time to T=1.1 (cid1 expired, cid2 not expired)
+        monkeypatch.setattr(time, "time", lambda: 1.1)
+        manager.cleanup_expired()
+
+        assert cid1 not in manager.get_expected_cids_for_peer(peer1)
+        assert cid2 in manager.get_expected_cids_for_peer(peer1)
+        assert peer1 not in manager.get_dont_have_peers(cid1)
-- 
2.50.1 (Apple Git-155)

